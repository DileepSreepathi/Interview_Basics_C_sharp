using System;
using System.Collections.Generic;
using System.Text;

namespace Interview_Basics_C_sharp
{
    class GarbageCollection
    {
       // Dispose() -used to free or release unmanaged resources(e.g., files, database connections) when called explicitly.
       // Finalize() -used to free the unmanaged resources on the code before garbage collection happens.
    }
}
